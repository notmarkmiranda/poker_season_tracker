RSpec.describe "PATCH /api/v1/links" do
  context "with existing players" do
    xit "updates an existing game" do
      game = create(:game)
      participant1, participant2 = create_list(:participant, 2)
    end
  end

  context "with some new players" do
    xit "creates new players and updates an existing game" do
    end
  end
end
